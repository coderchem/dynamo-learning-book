# 面经-Dynamo（题库与追问链）

> 配套《Dynamo 学习手册》与《导学-Dynamo.md》使用。
> **使用规则**：每题先自己作答再看口播；口播是"源码研究视角"的第一人称
> 叙述（情境→归因→动作→结果），所有技术结论可在 `ai-dynamo/dynamo@61e9184`
> 源码复核。标 🔬 的句子涉及动手实验——跑过 ch05/ch19 的 mocker 环境后
> 才建议在面试中这样说。
> 主问题按面试出现频率从高到低排列。

## 1. 项目简介（简历可用）

对 NVIDIA 开源分布式推理框架 Dynamo（v1.5.0）完成端到端源码级拆解，产出
26 章中文学习手册并公开建站：覆盖分离式推理、KV 感知路由的代价打分模型、
多层 KV 缓存管理、事件流可靠性、K8s 部署编排，全部结论锚定具体 commit
的源码路径与默认值。

## 2. 简历 bullet

- **分布式推理架构解析：** 针对 LLM 推理引擎与集群编排层之间缺少代码级
  中文资料、网上大量内容停留在已废弃旧入口的问题，对开源框架 Dynamo 的
  三层技术栈做端到端源码拆解：还原组件/端点抽象、三条通信通路与请求
  八阶段链路，并以"锚定单一 commit + 路径逐一核验"的方法保证结论可复核，
  产出 26 章结构化手册与公开站点，支撑推理基础设施方向的深度技术问答。

- **缓存感知路由机制拆解：** 针对缓存感知路由在公开资料中只有概念描述、
  缺少可验证决策逻辑的问题，从框架内置 worker 选择器源码还原出完整代价
  打分模型：分层缓存抵扣（显存/CPU/磁盘三档计价）、负载热点衰减、污点
  约束乘子与温度采样选worker 的公式、全部权重默认值及环境变量开关，并
  梳理七种路由模式与逐级降档的排障路径，形成可对照源码逐行验证的路由
  知识体系。

- **前缀索引与事件流一致性建模：** 针对"索引数据怎么维护、允许什么程度
  的不一致"这一分布式缓存核心疑问，拆解前缀索引的双数据结构（压缩前缀
  树与位置优先哈希、含跳跃优化）及其并发形态与选型边界，并还原
  "尽力而为事件流 + 单调序号 + 游标状态机检测缺口 + 快照重放兜底"的
  可靠性设计，明确"索引错误只造成次优路由、不造成错误结果"的一致性
  边界。

- **分离式服务与传输成本建模：** 针对 prefill/decode 分离部署"何时划算"
  的决策盲区，拆解分离接力的三阶段协议、条件分离的旁路判定与在途传输
  的中止保护语义，建立 KV 迁移字节数估算模型与网络带宽敏感性分析方法，
  给出短请求、小规模、无 RDMA 网络三类不适用场景的判断依据。

- **知识体系化与文档工程：** 将源码研究固化为可交付工件：按"适合谁读/
  前置/耗时/学习目标/自检/下一步"的章节契约组织 26 章内容，配套学习
  路径、术语表与全书自检清单，静态站点提供侧边栏导航、全文搜索与图表
  渲染；命中率与迁移收益等量化结论明确标注待测项与测量方法，不编造
  未验证数据。

## 3. 面试问题（19 主问 + 追问链）

### 主题 A：架构与基础（频率最高）

#### Q1. 为什么推理集群需要在引擎之上再加一层编排框架？它和 vLLM 的分工边界是什么？

**口播**：我的理解来自对 Dynamo 源码的拆解。单台引擎解决的是"请求到我
这里之后怎么算得快"，但真实集群里有五个引擎回答不了的问题：上百个请求
发给哪个实例、prefill 和 decode 两类池子之间 KV 怎么衔接、负载涨了各加
几副本、某个实例挂了在途请求怎么办、这整套怎么部署到 K8s。这些问题的
答案合起来就是编排层：它不执行模型前向、不含任何 attention kernel，而是
负责入口、路由、KV 传输、分层缓存、扩缩容和部署。我在手册里把边界总结
成一句话：引擎管单实例执行，编排层管集群协同；换引擎时入口、路由、缓存
层零改动，改的只是部署清单里 worker 组件的类型。这个分层我认为和 Service
Mesh 之于后端服务是同构的——值不在算得快，在全局决策。

- 追问 A1：那 HTTP 前端为什么用 Rust 写，Python 只做入口？
  **口播**：我拆启动链路时确认过这条路径：Python 的前端包 main 函数只做
  参数解析和运行时构建，然后通过 PyO3 绑定调进 Rust 的入口函数，由 Rust
  起真正的 HTTP 服务器，OpenAI 兼容的处理器在一个约九千行的 Rust 文件里。
  这样做的原因我认为有两个：一是前端是全集群流量的串行咽喉，token 化、
  路由问询都在这条路径上，延迟抖动会被所有请求放大，性能敏感路径放 Rust
  和把核心放 Rust 是同一个决策；二是 chat 模板、分词这些逻辑要和路由共
  享同一份 token 视图，放 Rust 里可以让 Python 组件和 Rust 组件用同一套
  实现，避免两套语言对同一请求算出不同的 token 序列。

- 追问 A2：集群内部的通信怎么组织？
  **口播**：我把它的通信按用途拆成三条通路，这个拆法帮我理清了很多设计。
  控制面走 etcd，承担服务注册、lease 保活和分布式锁，频率低但要强一致；
  请求面搬 token 流，有 TCP 直连、ZMQ 和 NATS 几种形态，按部署规模选；
  事件面广播 KV 事件，做成可插拔，大集群倾向独立 ZMQ 通路避免和请求抢
  资源。关键认知是三条通路的服务等级不同：请求不能丢，事件允许尽力而为
  ——丢失靠后续事件和未命中反馈自愈。我在手册里给了一句记忆法：请求面
  搬 token，事件面搬"谁缓存了什么"，控制面搬"谁还活着"。

#### Q2. prefill 和 decode 为什么要分开对待？

**口播**：这两个阶段的资源特征是相反的。prefill 把整个 prompt 一次喂入，
大量 token 并行算注意力，计算密集、吃算力，产出是 prompt 的 KV；decode
逐 token 生成，每步都要读全部历史 KV 再算一个 token，访存密集、吃显存
带宽，batch 越大带宽利用率越高。放同一批 GPU 上会互相干扰：一个长
prefill 进来，在途 decode 请求的每 token 延迟立刻抖动，因为两类负载抢的
资源不同。由此能推导出三个系统设计：chunked prefill 把长预填充切块和
decode 混排是缓解；彻底的解法是分离式部署，两类池子独立扩缩容，中间用
KV 传输衔接；再往上是按 SLO 做池子比例的自动规划。我拆源码时还注意到
一个细节：路由打分里 prefill 池和 decode 池的负载项用了不同的度量，前者
按 token 计算量、后者按并发和显存，因为两者本来就不是同一种资源。

- 追问 B1：TTFT 和 ITL 分别由什么主导？你怎么优化各自？
  **口播**：首 token 延迟由 prefill 主导，优化方向是减少要算的 token 数
  ——前缀缓存命中越多、算得越少，这就是 KV 感知路由的价值；分离部署下
  还要加一项 KV 迁移时间。每 token 延迟由 decode 主导，优化靠提高 batch
  并发摊薄访存、控制 decode 池不过载。我在拆打分公式时发现这两类优化
  在代码里就是两个代价项：prefill 负载项可以被缓存命中抵扣，decode 负载
  项独立计价，路由器实际是在为每个请求做一次"这次 TTFT 和 ITL 谁更划算"
  的隐式权衡。

- 追问 B2：什么负载不适合分离部署？
  **口播**：我在手册里整理过判定表。第一类是短进短出，比如分类请求，
  迁移开销占比会超过收益；第二类是一两个节点的小规模，两池各自闲置率
  上升得不偿失；第三类是没有 RDMA 或 NVLink 的网络，KV 走 TCP 会把分离
  收益吃光，我按公式粗算过，70B 级模型四千 token 的 prompt KV 约六百多
  MB，25G 网卡要传两百多毫秒，直接把 TTFT 打爆。有意思的是框架自己也有
  这个判断：有个条件分离机制，按请求特征动态决定走分离还是旁路到聚合
  路径，说明作者也认为分离是优化手段而不是信仰。

#### Q3. 讲一个 OpenAI 兼容请求的完整生命周期。

**口播**：我按源码把一条流式 chat 请求拆成八阶段。入口是 Python 前端 CLI
调 PyO3 绑定进 Rust 起 HTTP 服务；请求进来先按内部协议类型解析校验；然
后是前处理，应用 chat 模板、处理多模态输入和工具调用改写；第三步分词，
tokenizer 由服务发现层随 worker 注册一起提供，保证前后端 token 视图一
致；第四步路由决策，拿 token 块序列查前缀索引、综合负载选出目标
worker；第五步经请求平面投递到后端的生成循环；第六步 token 流逐批流回；
第七步聚合器把 token 流收口成 SSE 分片和用量统计；第八步写回客户端，
断连有过载有专门的语义处理。我在本地用假引擎把这条链路完整跑通过 🔬，
加日志观察各阶段顺序，对每个阶段的文件位置都标了源码锚点，所以被追问
任何一段我都能落到具体代码。

- 追问 C1：分词为什么必须在前端做，不能交给被选中的 worker？
  **口播**：因为路由和索引都工作在 token 块级别。路由查询的 key、块哈希
  的输入、命中深度的计算，全都依赖 token 序列；如果分词发生在 worker，
  路由器就得先选一个 worker 去问 token 数，选错了还得重来。更关键的
  是一致性：块哈希是跨 worker 的身份系统，同一请求在路由侧和引擎侧必须
  算出同一串块哈希，前端统一分词加统一块边界配置，才保证"路由说命中了
  80%，引擎那边真的有这 80%"。我在手册里把分词错误的后果定性为路由正
  确性问题而不是文本问题，就是从这个依赖链来的。

- 追问 C2：非流式和流式在哪收口？
  **口播**：都在聚合器这一层收口，区别只是时机。流式请求每收到一批
  token 就增量聚合成一个 SSE 分片写回，同时维护用量统计和结束原因；非
  流式则是聚合器把整个 token 流攒完再一次性构造完整响应。聚合器还负责
  多种协议表面的共享逻辑，所以换 gRPC 或其他兼容协议时，路由、投递、生
  成这些主干完全复用，只有入口解析和出口包装换掉。我理解这个设计的意
  图是把"协议多样性"限制在最外两层，中间的管线保持单一。

#### Q4. 你怎么判断一份推理框架资料是不是过时的？

**口播**：我在这个项目里专门建了一套版本考据方法，因为踩过坑：网上大
 量教程还在教旧版的一体化启动命令，而新版根本没有这个命令，组件改成每
  个独立进程启动，或由 K8s 从声明式资源渲染。我的判断标准有几条：一看
  启动方式，出现旧命令即过时；二看协议类型的出处，有些版本协议类型在
  仓库内，新版拆到了外部 crate；三看部署模型，旧资料没有声明式推理图
  这一层。更重要的是方法论层面的：我手册里所有结论都锚定单一 commit，
  行号会漂但路径和符号名相对稳定，读者拿自己的分支对照时先查提交历史。
  我把这条写进了全书的五条阅读军规，第一条就是"源码才是唯一真相"。

- 追问 D1：为什么不直接信官方文档？
  **口播**：官方文档回答"怎么用"，是面向使用者的，更新节奏跟主干，但对
  "为什么这样设计、代码在哪、机制怎么运作"基本不覆盖。我的目标是能回答
  深挖式问题，所以必须回到源码。举个例子，路由打分的官方叙述只有一句
  "按缓存重叠和负载选 worker"，但真正的决策是一个有七八个权重项的代价
  公式，默认值、请求级覆盖、温度采样这些细节只存在于代码和测试里。我
  的做法是文档当地图、源码当地质，两者冲突时信源码，并在手册里标注每
  个论断的文件位置，方便任何人复核我说得对不对。

- 追问 D2：协议类型为什么要拆到外部 crate？
  **口播**：我理解是解耦发布节奏。请求响应的协议结构是多端共享的契约：
  Rust 前端、Python 组件、独立路由服务、事件整合器都要用。如果放在主
  仓库的某个 crate 里，每改一个字段就得动整个工作区版本；拆成独立发布
  的协议 crate 后，引擎侧的对接代码可以只依赖小体积的契约包，不用拉整
  个框架。这和后端把 API 的 proto 定义拆成独立包给客户端用是一个道理。
  我在手册里专门提醒读者别在主仓库里找协议 crate，因为我一开始也找错
  了地方。

### 主题 B：KV 感知路由（本项目的独有深度）

#### Q5. KV 感知路由的"感知"从哪来？完整信息流讲一遍。

**口播**：核心是信息流反转：不让路由器轮询 worker，而是 worker 主动广播
"我缓存了什么"。我拆出来的链路是五段：引擎侧的 KV 块事件先经过一个整合
器，把引擎原生事件转成统一线格式；然后发布器做两件卫生活——攒小批降
  开销、去掉被后续事件覆盖的重复项；再经可插拔的事件平面广播出去；消费
  侧的索引服务把事件应用到前缀树。还有一个容易被忽略的细节：事件源本身
  也注册到服务发现里，路由是从 etcd 发现当前活着的事件源再订阅，worker
  生死和事件源存亡绑定。与 KV 事件平行还有一条负载指标流，一秒级快照。
  索引回答"去哪最省"，负载回答"去哪不挤"，两条流在打分器里合流。

- 追问 E1：事件丢了怎么办？允许丢吗？
  **口播**：允许，这是刻意设计。判断依据是错误的代价：索引"少记"只是
  错过一次命中，多算一遍 prefill，次优但正确；索引"多记"会在路由到目标
  后未命中，引擎重算，索引被反馈修正。也就是说索引只是优化输入，不是
  正确性守门员。这个性质让整条链路可以大胆做批处理、去重、近似结构。
  但"允许丢"不等于"不检测"：每个事件带单调递增的序号，消费侧有个游标
  状态机，收到的事件号跳了就是缺口，旧号重放就是陈旧直接丢弃；缺口走
  恢复路径，可以找 worker 的恢复端点拉索引快照，或者导出重放。

- 追问 E2：批处理和去重各自防什么病？
  **口播**：批处理防的是通路开销放大：一个 worker 每步都可能产生块级事
  件，逐条广播的话小消息的固定成本会吃掉事件平面，所以攒批，代码里有
  单批块数上限一百二十八和十五秒的强制截断上限——前者防单条消息过大，
  后者防低流量时事件无限延迟。去重防的是同一批内的无效信息：同一个块
  先存后删，只需发终态，否则索引要白白应用两次。这两个机制都会让索引
  短暂"更旧"，但既然语义本来就允许尽力而为，这个代价是设计内化的。

#### Q6. 前缀索引为什么用 radix tree？内部怎么工作？

**口播**：需求是"对任意请求的 token 序列，快速回答哪些 worker 缓存了它
的最长前缀、命中几块"。整串做 key 的哈希表只支持全串命中，长 prompt 下
永不命中；按块切分后建压缩前缀树，路径按块组织，任意祖先节点都是一条
已缓存前缀，系统提示词、多轮对话、共享 few-shot 天然复用。我拆过它的
内部结构：每个节点带压缩边、按序列哈希的边索引、每个 worker 的覆盖深度
记录和子节点表。查询就是沿子节点表下行、每步拿 worker 覆盖集合和候选
求交，记录每个 worker 掉队的深度。插入是事件驱动的，事件里天然带"父块
哈希加块链"，正好沿树下行建边。整棵树还能反向导出成事件序列，所以快照
和恢复用的都是"空树加重放"这一个通用机制。

- 追问 F1：为什么按 16 或 64 token 一块，不按单 token？
  **口播**：这是个粒度权衡。按单 token，树深等于序列长，指针追逐的访存
  次数和哈希计算量都按序列长线性涨，长上下文下查询延迟不可接受；按块
  则树深除以块大小，查询和事件量都骤减。代价是块内部分 token 变化会让
  整块失效，前缀复用的粒度变粗。块大小还是路由、前端分词、引擎三方的
  共同约定，任何一边不一致就会出现"路由说命中了但引擎没省"的经典故障，
  我在手册里把它列为排查三点之一。引擎侧的块大小通常由其页式注意力实
  现决定，路由侧只是对齐。

- 追问 F2：除了树还有别的结构吗？
  **口播**：有，官方设计文档里还有个位置优先的扁平哈希结构，键是"位置
  加块哈希"的复合键。它有个跳跃优化我很欣赏：查询时不是逐位置查，而是
  每隔三十二个位置跳一次做存活检查，全员还在就继续跳，有人掉队才回扫
  这一段找精确掉队点，把查询从线性降到序列长除以跳距。代价是它需要能
  增量算序列哈希，而树结构只把序列哈希当不透明 id，所以引擎用什么哈希
  算法都能接。选型上官方给了决策树：默认用并发树加线程池的形态，worker
  数上千才考虑按分叉分片，分片形态为了正确性牺牲了近似修剪的能力。

#### Q7. 块的身份怎么定义？跨 worker 怎么保证一致？

**口播**：一个块有四种标识：块内哈希标识内容、序列哈希标识"某条历史里
的第几块"、加 worker 加位置定位到"谁那里、第几格"。跨 worker 一致靠的
是块内哈希的确定性：同样 token 前缀在任何 worker 上算出同一哈希，身份
是内容寻址的。但"纯内容"还不够，有三重盐按需混入：LoRA 适配器名要
长度前缀后拼进去，因为不同适配器对同样 token 算出的 KV 不同，混了就是
答错级别的故障，这一步在引擎侧完成；缓存命名空间做租户级隔离；还有
带密钥的跟踪哈希，把"能构造合法块哈希"的能力限制在持密钥的路由域内。
最后这个防的是侧信道：块哈希本来是确定性的，任何人都能算出"某段系统
提示词的块哈希"，再观察路由行为做前缀探测；加了密钥，外部构造的哈希
在路由域内无效。

- 追问 G1：位置敏感性是怎么回事？同一块出现在不同位置不能复用吗？
  **口播**：大体不能随意复用，根因是注意力的因果性：KV 能复用的前提是
  "从序列头开始连续相同"，一个块即使内容相同，如果前面挂的前缀不同，
  它在注意力里的上下文就不同，算出的 KV 也不同。这就是序列哈希要累积
  计算、树要按路径组织的原因。事件格式里还带了块的绝对起始位置字段，
  支持按位置重放，匹配侧有位置敏感的变体处理"同块不同位"的价值差异。
  我理解这套设计是把"前缀可复用"这个负载特征严格限制在因果前缀的语义
  内，而不是宽松的内容匹配。

- 追问 G2：如果让你加一层远端缓存池，哈希身份要动吗？
  **口播**：不用动身份系统，要动的是索引的分层记账。身份是内容寻址的，
  块在显存、内存还是远端，身份不变；变的是"谁持有、在哪一层"的记录。
  现有索引已经有分层变体，事件的包装结构里带存储层级字段，KV 下放到
  CPU 产生的是同一条管线里的层级变更事件而不是另一套身份。真正要小心
  的是跨数据中心场景下的哈希域一致性：如果两个区域用不同的盐或不同的
  跟踪密钥，一边"有"的东西另一边永远查不到，这是跨区命中率异常的第一
  排查点。所以我的答案是身份不动、记账分层、密钥域要对齐。

#### Q8. 路由决策的打分公式讲一遍，每项为什么存在？

**口播**：我从选择器源码把公式还原出来了，本质是个减分制的代价模型：
  一个 worker 的代价等于 prefill 负载项加 decode 负载项加在途请求成本，
  其中 prefill 项先被缓存抵扣再乘缩放系数。缓存抵扣是分层计价的：显存
  命中一点零、CPU 零点七五、磁盘零点二五，正好对应取回延迟阶梯；共享
  缓存默认零，开了按本地一半计价，因为还要网络取回。有个热点衰减设计
  我特别想讲：缓存抵扣会随"相对最闲 worker 的超额 prefill 积压"衰减，
  公式是分式的有理衰减，防止所有请求涌向缓存最热但也最忙的 worker，
  用缓存局部性换预填充均衡。decode 池有特例分支，缓存抵扣直接从 decode
  积压里减，因为 decode 侧命中意味着省掉取回。最后按温度选择：零温是
  严格取最小代价加蓄水池随机破平，正温做 softmax 采样引入探索。污点偏
  好以乘子作用于代价，像 K8s 的污点容忍。

- 追问 H1：T=0 时两个 worker 严格同分，选谁？为什么这么设计？
  **口播**：蓄水池抽样随机选。我一开始以为是取第一个，读了实现才发现
  维护了同分计数，第 n 个同分者以 n 分之一概率替换当前最优。这样设计
  是因为如果不随机破平，所有配置相同的 worker 在同分场景下会永远偏向
  排序靠前的那个，负载信息稍有滞后就形成羊群效应。这个行为有专门的单
  测试钉死，我印象很深，因为它把一个容易被当实现细节忽略的选择上升成
  了被测试保护的契约。温度大于零时换成 softmax 采样，代价低的概率高，
  温度越高越随机，动机同样是给路由加探索来对抗负载指标的滞后性。

- 追问 H2：为什么 CPU 命中是 0.75、磁盘是 0.25？这个阶梯合理吗？
  **口播**：我的理解是它编码的是"取回成本相对重算成本"的比值：显存命
  中几乎免费所以全额抵扣；CPU 命中要一次主机内存搬运，价值打七五折；
  磁盘再慢一档打二五折；跨 worker 的共享缓存要过网络所以默认只有本地
  一半。这个阶梯是否精确合理要看硬件——NVLink 域内和跨机 RDMA 的取回
  成本差很远，我把它理解成一组可调的先验而不是物理常数，源码里也都是
  环境变量可改的。如果我的场景是全 NVLink 单机多卡，我会调高共享层权
  重；如果是慢网络跨机，会把远端命中权重压低甚至关掉，让路由更偏向本
  地重算。

- 追问 H3：请求级覆盖是什么？有什么用？
  **口播**：打分权重除了全局配置，还能按请求覆盖。我拆过一个具体应用：
  分离部署的 decode 路径默认把缓存抵扣系数通过请求级覆盖清零，做纯负载
  路由；但条件分离的旁路场景又保留正向抵扣，让缓存热的 decode worker
  优先，同时仍计 decode 积压。温度也有请求级覆盖。这个设计的价值是把
  "策略随场景切换"做进了数据面而不需要重启或全局开关，相当于给每类
  请求一个小型策略命名空间。面试里我把它类比为按请求下发的 feature
  toggle，但作用域是路由权重。

#### Q9. 路由有哪些模式？线上出问题你怎么用它排障？

**口播**：模式一共七种：轮询是默认，还有随机、两选一取闲、纯最闲、按
设备类型加权、直连，以及完整的 KV 感知模式。有些高级特性强制要求 KV
模式，比如基于性能模型的负载估计、独立索引服务、条件分离，参数校验会
直接拒绝。我的排障方法是逐级降档：KV 降到最闲，如果恢复正常，问题在
索引或打分引入的输入；再降到轮询，如果又正常，问题在负载流；如果一直
不正常，问题在更底层的服务发现或传输。每降一级就剥离一层输入，哪一级
开始恢复，问题就在那一级新引入的依赖里。这个方法的前提是知道各级模式
分别消费哪些输入，我在手册里画了对应表。🔬 我在本地假引擎环境里切换
过模式对比选择日志，降档时日志里的选择理由变化能直接看到。

- 追问 I1：KV 模式比轮询多了哪些故障面？
  **口播**：三类。索引数据面：事件断了或批处理积压，索引过旧，命中失真
  ——表现为命中率异常低，查事件平面订阅和索引日志。一致性面：前端分词、
  块边界配置、引擎侧的块提示三者不一致，表现为"日志说命中了但延迟没
  降"，这是最隐蔽的，要对着三方配置逐项核对。决策面：权重配置不当，
  比如热点衰减为零又把抵扣系数拉满，流量全涌向缓存最热的 worker，表现
  为单 worker 过载其他闲置，看打分公式的分解日志能直接对账。每类我都能
  给出对应的日志位置和配置项。

### 主题 C：调度、驱逐与容量

#### Q10. 路由层的队列调度用什么策略？怎么选？

**口播**：内置两种：先来先服务是默认，带优先级加塞，优化的是尾部首
  token 延迟，长请求不会饿死但平均等待差；另一种是加权最短处理时间，
  就是调度论里的 Smith 法则，按处理时间除权重升序服务，数学上最优化的
  是加权完成时间和，映射过来就是平均首 token 延迟最优。选择取决于 SLO
  写的是 P99 还是 P50。触发排队也有明确条件：可配一个水位阈值，当所有
  候选 worker 的预填充 token 水位都超过其批处理容量上限的该比例时，请
  求进队列而不是硬塞给最不忙的那个，这是过载背压的第一道闸，配合队列
  拒绝的出口构成完整的过载语义。我欣赏这个设计的地方在于它把"排队"做
  成了显式可配的行为，默认关闭，而不是隐式的全量排队。

- 追问 J1：Smith 法则为什么最优？直觉解释。
  **口播**：直觉是交换论证：假设最优序列里相邻两个请求"长的在前短的在
  后"，交换它们不改变其他请求的完成时间，但短的那个提前完成、长的推迟
  相同量，因为短的权重时间比更高，总加权完成时间严格下降，矛盾。所以
  存在最优解满足按权重时间比非降排列。代价是尾部：长请求被无限推后，
  P99 恶化，极端下饿死。所以推理服务的默认是先来先服务保尾部，把
  Smith 留给明确优化均值的场景，这个取舍我在面试里会主动讲清楚，因为
  它体现了"SLO 决定调度器"而不是反过来。

- 追问 J2：优先级加塞不会饿死低优先级吗？
  **口播**：经典问题，经典解法是老化或权重随等待时间增长的补偿，源码
  里的优先级实现带跳变字段和严格优先级档位，属于显式的调度输入而不是
  隐式行为。我理解在推理场景里优先级的主要用途是运维动作，比如灰度流
  量、内部探活请求插队，持续时间短，饿死风险本来低；真要长期混合多档
  优先级，就该上带公平份额的加权队列而不是裸的严格优先级。这个问题我
  在书里的自检题也出了，因为它是追问链上必被问的一环。

#### Q11. KV 缓存满了扔谁？讲讲驱逐体系。

**口播**：我先纠正一个我自己的初始误解：路由索引只是记账员，真正扔 KV
  的是 worker 侧的缓存管理，两级的职责要分开。路由侧只有无事件模式下
  的 TTL 过期兜底。worker 侧的驱逐后端是一族：先进先出、最近最少使用、
  分代的多队列 LRU，以及谱系感知驱逐。谱系驱逐利用的是块哈希的链式结
  构天然编码的"这块从哪条序列分叉出来"：多轮对话的祖先块大概率被下一
  轮命中所以保，一次性输出的叶子块先扔，这是对话负载相对纯 LRU 的关键
  增益。还有一个我特别想讲的分层认知：准入和驱逐是正交决策——新块想进
  缓存先过频率门槛，这是 TinyLFU 风格的准入；"让不让进"和"把谁扔出"
  组合使用，抗扫描污染的效果比单靠驱逐好。

- 追问 K1：驱逐怎么反过来影响路由？
  **口播**：因果链是：驱逐后端决定哪些块消失，释放事件发出去，索引摘
  记，下一次查询的命中深度变化，打分重排。所以换驱逐策略等于隐式改写
  未来所有请求的打分输入——你扔掉的缓存决定了之后命中不了什么。举
  例：把 LRU 换成 FIFO，在一轮对话间隔较长的负载下会把还热着的前缀提
  前扔掉，命中率掉，路由器什么都没改却"变笨了"。所以我在手册里把驱逐
  策略称为隐式路由策略，调驱逐参数时要盯着命中率指标看，而不是只看
  显存水位。

- 追问 K2：多轮对话负载下谱系驱逐为什么优于 LRU？
  **口播**：因为对话的价值分布不在"最近"这个维度上而在"谱系位置"上：
  系统提示词和最早几轮对话的祖先块，哪怕很久没被命中，下一轮请求一来
  全都要用；而最近生成的叶子块，除了最后一条几乎没有复用价值。LRU 按
  时间局部性排序，恰好把高价值的旧祖先和低价值的新叶子放错了位置；
  谱系驱逐按家谱深度和分叉结构判断，祖先优先保留。无差别的场景是纯
  一次性负载，没有多轮结构，谱系信息不增值，LRU 的简单性反而占优。

#### Q12. 路由怎么跟踪在途请求？长生成请求的占地怎么算？

**口播**：路由侧有一本在途账本，生命周期三步：请求被接受时登记，预填
  充完成时标记（这是分离接力的对接点），结束时释放，释放带条件校验
  "仍在原 worker"防止迁移后误删。账本之上有个我很想讲的设计：输出占地
  预测。decode 请求的 KV 占地是持续增长的，只看当前块数会系统性低估长
  生成请求的未来成本，所以有个开关开启后，路由在生成过程中按占位块加
  分数衰减的方式向目标的输出长度插值，让打分看到"这条请求最终会占多
  大"。关闭这个开关时，长生成请求在打分里被低估，方向是低估其 decode
  显存压力，导致过量接入后显存吃紧。这个思想和缓存侧的谱系驱逐构成
  同一原理的两端应用：一个用历史价值，一个用未来占地。

- 追问 L1：prefill 侧的在途 token 为什么由路由记账而不是 worker 上报？
  **口播**：因为只有路由知道请求何时被接受。worker 感知到请求时，它已经
  在队列里排了一会了，上报永远是滞后的；而打分公式里的热点衰减用的正
  是"相对最闲 worker 的在途 token 超额量"，这个量必须实时才有效。所以
  设计上路由在接受请求的瞬间就记账，worker 只上报它那边更容易观测的
  decode 块数和 KV 占用水位，两者在指标流里是不同字段，路由侧的预填充
  token 字段在 worker 上报里恒为空。代价是路由和引擎的账本可能漂移，
  靠释放时的条件校验和定期对账兜底。

### 主题 D：分离式服务与 KV 传输

#### Q13. 讲讲 PD 分离的完整流程，以及"要不要分离"怎么判断。

**口播**：一条请求在分离模式下的三阶段：先进预填充池算完 prompt 的 KV；
  然后是接力，预填充路由把序列激活交给 decode worker，同时 KV 块通过传
  输层跨节点搬到 decode 侧；decode 从最后一个 token 续算生成。角色模型
  上有四种 worker：预填充、解码、编码、聚合，编码角色服务多模态链路，
  视觉编码器是独立的一类计算。要不要分离我判断三件事：负载特征上，短
  进短出迁移占比高不划算；规模上，一两节点两池闲置率上升得不偿失；网
  络上，没有 RDMA 级别带宽，KV 搬运时间直接进首 token 延迟。框架自己
  也有条件分离机制，按请求特征动态旁路，还有请求级的绕过远端预填充注
  解，说明"分离是优化不是信仰"是内建到代码里的立场。

- 追问 M1：接力窗口为什么是最脆弱的点？
  **口播**：接力窗口是"预填充已完成、解码未开算"的区间：KV 已经花算力
  算出来了但还没产生任何 token，此时任何一端故障，已投入的 prefill 计算
  全部作废，请求要从头再来，比普通在途请求多损失一整段算力。窗口长度
  由 KV 迁移时间决定，所以传输带宽不只影响延迟还影响故障暴露面。配套
  的保护我拆到两个：解码侧的在途中止防护，传输未收敛时不响应强杀，防
  止留下悬空的远端内存句柄；序列账本在预填充完成时打标，故障恢复时能
  区分"prefill 已完待接力"和"prefill 进行中"，采取不同的重放策略。

- 追问 M2：条件分离按什么判定？
  **口播**：配置面有三个维度的阈值：按有效输入序列长度的绝对阈值和比
  率阈值，加预填充池与解码池各自的繁忙阈值。逻辑是请求太短直接旁路到
  聚合或解码路径，池子繁忙时也旁路，避免分离链路成为瓶颈。我理解它的
  本质是把"分离收益为正"这个条件显式化：分离的收益是消除干扰加独立扩
  缩容，成本是接力协调加迁移时间，短请求或拥塞时成本项反超。这种"优化
  路径带逃生门"的模式在这套代码里反复出现，比如路由的模式降档、请求
  级的远端预填充绕过，我认为是生产级系统区别于论文实现的标志。

#### Q14. KV 迁移要搬多少数据？给我一个估算方法。

**口播**：公式是两倍的层数乘 KV 头数乘头维乘序列长乘精度字节，单条请求
  的 prompt KV 就是这个量。我习惯用一个大模型实例锚定数量级：七十 B 级、
  八十层、八个 KV 头、一百二十八维、FP8，四千 token 的 prompt 算出来约
  六百七十 MB。然后按网络有效带宽除：四百 G 的 IB 网卡有效约三十五 GB
  每秒，传十九毫秒可以接受；二十五 G 以太网有效两点五 GB 每秒，要两百
  六十多毫秒，首 token 延迟直接爆炸，分离没有意义。这个估算我在手册里
  配了敏感性表，也说明为什么分离部署的前置检查里网络规格排第一。反向
  也成立：命中前缀省下的是重算的算力，换来的是传输字节，两者单位不同，
  比较时要把算力折成时间、带宽折成时间，在时间维度上比。

- 追问 N1：既然命中可以省计算，为什么不全量迁移共享？
  **口播**：因为迁移有成本而重算有并行度。传输是串行占带宽的：搬六百
  MB 就是实打实的两百多毫秒，而重算四千 token 在大 batch 下可能只要几
  十毫秒，算力富裕时"重算比搬运快"是成立的。所以决策不是"有缓存就迁"
  而是"迁移时间小于重算时间才迁"。打分公式里的分层计价就是这个比较的
  货币化：显存命中全额抵扣因为取回近于免费，远端命中打五折因为要过网
  络。极端情况下网络很差时最优策略是各 worker 各自重算，前缀缓存只在
  worker 内部生效，这也是路由权重可调的原因之一。

- 追问 N2：迁移途中客户端断开了怎么办？
  **口播**：我拆过 vLLM 后端的处理：解码 worker 收到中止请求时如果传输
  还在途，会走延迟中止——先把中止挂起，等传输收敛再执行清理。原因是
  强杀会留下悬空的远端内存注册句柄，块表和实际内存状态脱节，后续的传
  输和回收都会出错。这个细节我印象深是因为它体现了一个通用原则：涉及
  异步资源的操作，取消语义必须是"收敛后取消"而不是"立即取消"，和分布
  式系统里优雅退出的排水是同一个思想。配套还有请求级的绕过注解，允许
  单条请求不依赖远端预填充结果直接本地算，作为迁移链路异常时的逃生门。

#### Q15. NIXL 是什么？一次迁移的两端怎么协作？

**口播**：NIXL 是兄弟仓库里的高性能传输抽象库，同一套 API 盖住 RDMA、
  NVLink、UCX 等后端，为推理负载的大块 KV 搬运设计。框架不直接写
  verbs，全部经它。一次迁移的协作五步：源端先把 GPU 内存注册进 NIXL 的
  agent，注册开销靠内存池化摊到 worker 生命周期；然后块句柄表走控制通
  路发过去，这是毫秒级的小消息，描述哪些块、什么布局、远端地址描述子；
  目的端就绪回执后，KV 字节走数据通路一次或分批传输；完成通知走独立的
  通知机制；解码侧等到通知才算就位可以开算。控制与数据分离是关键设计：
  句柄表要可靠有序，字节流要吞吐。传输执行器有三个实现盖住三种拓扑：
  跨节点走 NIXL，同机多卡走 CUDA 通道，主机内走内存拷贝，上层不感知物
  理位置。

- 追问 O1：为什么内存注册要池化？
  **口播**：因为注册贵。RDMA 场景下把一段显存注册成可被远端直访的区
  域，涉及 pin 页、建映射、同步给网卡，单次是毫秒级成本；如果每次传输
  前注册、传完注销，迁移的有效带宽会被注册开销吃掉大半。所以做法是把
  KV 用的显存做成 arena 或池，进程起来时一次注册反复使用，块只是在池
  内的位置分配。这也解释了为什么 KV 管理器要自己管内存布局而不是交给
  引擎默认分配器——布局决定了可注册性和传输效率。我在手册里把这条和
  操作系统的页池、网卡驱动的缓冲池归为同一类工程手法。

#### Q16. 多层 KV 管理怎么设计？讲讲你的理解。

**口播**：我把它类比为操作系统的换页子系统，分三层职责。逻辑层做记账
  和策略：块的状态机、活跃池与不活跃池、驱逐后端族和准入控制，不知道
  字节在哪个设备。物理层管布局和搬运：KV 块的内存布局、本管理和远端管
  理、以及三种传输执行器。引擎层是决策者：水位驱动的下放和拉回、对象
  存储层、多副本时的主从协调，把前两层串成完整生命周期。两条核心控制
  流：显存水位超阈值触发下放，逻辑层选块标记、物理层搬运、事件通知索
  引更新分层记账；反向是命中下层时预填充前先把块拉回显存，命中部分免
  算，首 token 延迟约等于迁移时间仍小于重算。索引的分层感知加搬运能力
  合起来就是容量弹性：命中率不再被显存封顶。

- 追问 P1：抖动（thrashing）在这套系统里是什么？怎么调？
  **口播**：刚下放的块马上又被请求命中要拉回来，或者上层频繁换入换出，
  搬运量暴涨而命中率不升。触发条件是水位阈值和工作集大小贴得太近：水
  位线压着工作集走，每次下放都放掉了马上要用的东西。调法是拉开水位和
  工作集的距离：调低下放触发水位留缓冲带，或者扩大下层容量让换页率下
  降。观测指标是各层命中率加下放拉回吞吐加迁移排队延迟，如果看到下放
  吞吐高同时上层命中率在掉，基本就是抖动。这和操作系统换页颠簸的诊断
  完全同构，所以我特别主张用换页的直觉来理解这套系统。

- 追问 P2：把价值阶梯完整默写一遍？
  **口播**：本地显存命中一点零，本地 CPU 层零点七五，跨 worker 的共享
  缓存零点五，本地磁盘层零点二五，无命中零。这四个非零项正好对应打分
  公式里缓存抵扣的四个分量，每个权重都是环境变量可调的先验。数字背后
  是取回成本的排序：显存访问近于免费，主机内存一次搬运，共享层要过网
  络所以默认按本地一半计价，磁盘最慢。我会主动补充：这是先验不是常数，
  NVLink 域内应该调高远端权重，慢网络应该压低甚至关掉共享层计价，让路
  由回到本地优先。能说出"阶梯可调和什么场景调什么方向"比背数字更重要。

### 主题 E：工程化与项目自述

#### Q17. 一个 worker 挂了，系统里依次发生什么？

**口播**：我把这个问题拆成五问来答。检测：没有独立心跳，靠 etcd 的租约
  保活，进程死了租约过期注册消失，谁在 watch 谁先知道。事件流完整性：
  消费侧游标状态机检测到缺口，走恢复路径，能拉快照就拉，不能就接受短
  暂不准。索引清账：摘除该 worker 的全部持有记录，节点可能保留给其他
  worker。在途请求：账本定位受损清单，带迁移排除的重路由——失败的
  worker 进排除集合再选下一个，排除链耗尽则显式报错而不是无限重试；
  释放时带 worker 条件校验防止误删迁移后请求的记账。平台层：K8s 侧有
  故障切换级联控制器防止二次雪崩——一个故障触发重路由把剩余 worker
  压过载、健康检查再误杀的连锁。这套行为在仓库里有专门的容错测试目录
  做验收，包括高可用、迁移、取消和金丝雀场景。

- 追问 Q1：游标状态机的四种状态分别怎么处置？
  **口播**：初始态是收到第一条事件，无法判定连续性，诚实接受并进入存活
  态；连续态是收到的序号正好是上次的加一，正常推进；缺口态是序号跳了，
  说明丢了一段，触发恢复——按代价升级，先试找 worker 的恢复端点拉索引
  快照，不行就整体导出重放，最坏接受索引短暂不准靠后续事件自愈；陈旧
  态是收到的序号小于等于已应用的，说明是重放或重复投递，幂等丢弃。我
  欣赏这个设计的地方是把"尽力而为"变成可检测的尽力而为，而且初始态的
  诚实——不假装第一条就能验证连续性——体现了对分布式时间戳语义的
  尊重。

- 追问 Q2：金丝雀暂停和直接杀 worker 有什么区别？
  **口播**：杀 worker 走的是租约过期、注册消失、索引清账的完整故障路径，
  在途请求走重路由，资源最终被 K8s 回收重建。金丝雀暂停是更精细的运维
  动作：某个数据并行档位的流量被暂停，但 worker 不摘除、注册还在、索引
  记账还在，恢复时直接放开流量，没有重建和预热成本。用途是版本灰度和
  热点隔离。对路由的影响差异在于：摘除是候选集合收缩，暂停更接近"允许
  集合里打了个标记"，配合白名单和 pin 机制实现。仓库里有专门的测试验
  证暂停档位的流量归零与恢复，说明这是被一等支持的运维能力而不是
  hack。

#### Q18. 这套系统怎么部署到 K8s？扩缩容的执行链讲一遍。

**口播**：核心是一个声明式的推理图自定义资源，描述有哪些组件、每个组件
  跑什么程序、几个副本、什么资源。操作器把它渲染成实际工作负载——每个
  组件就是一个独立进程，操作器不做任何推理逻辑，只是翻译；需要拓扑
  感知的 gang 调度时交给一个专门兄弟仓库的调度器，把分离的多节点组绑定
  到互联域。运行期有两层生死：K8s 管进程的有无，etcd 和 watcher 管服务
  视角的生死，两层不一致的窗口正是"扩了但没流量"问题的根源。扩缩容的
  执行链我特别想强调一点：规划器发现 SLO 违例后不是直接创建或杀掉
  Pod，而是修改声明式资源的副本数，由操作器调和、发现层跟上、流量才迁
  移——任何时刻集群状态都可从声明推导，这是把基础设施即代码的纪律
  贯彻到了弹性上。

- 追问 R1：Pod 显示 Running 但没有流量，怎么排查？
  **口播**：按两层生死的断点查，我有固定清单。第一断点：进程有没有把
  注册写进 etcd——没有就是启动卡在引擎初始化或连不上注册中心，查进程
  日志和网络。第二断点：frontend 的 watcher 有没有收到 join 事件——没
  有就查 watcher 日志和网络分区。第三断点：路由有没有把流量分过去——
  注册了也 watch 到了但没流量，查选择器是不是被负载项压住了，新 worker
  负载从零开始不该被压，那就查指标流有没有接上，指标没上来的 worker
  在打分器眼里可能被当成满载。三断点分别对应注册、发现、决策三段，
  每段我都能给出日志位置。

- 追问 R2：规划器为什么以 SLO 为目标而不是利用率？
  **口播**：因为推理负载下利用率是错误信号。GPU 利用率高可能恰是 batching
  充分的健康状态，按利用率扩容会把健康的省钱状态当成过载；反过来首
  token 延迟违例时利用率可能还不高，因为瓶颈在排队或前缀计算而不是
  计算。SLO 是用户可感知的 truth：延迟分布直接度量服务质量。所以规划
  链路是负载预测乘性能模型——给定副本数预测延迟分布——再在预算约束
  内选满足 SLO 的最小配置。性能模型可以让部署时先做离线扫描校准，这比
  在线试错便宜得多。

#### Q19. 介绍一下这个手册项目本身：为什么做、怎么做的、局限是什么。

**口播**：动机是我要补推理基础设施方向的深度，发现引擎层有好资料但编排
  层基本空白，而且旧资料大量过时，所以决定直接拆源码自己做体系化笔记。
  方法上我定了三条纪律：所有结论锚定单一 commit，路径逐一核验存在才写
  进去；每个论断给出文件位置和符号名，读者可复核；不确定的地方明确标
  注而不是含糊带过。过程中我借助了 AI 工具做大规模源码定位和初稿，但
  关键论断比如路由打分公式、游标状态机、配置默认值，我都回到源码逐行
  核对过才定稿。产出是二十六章加附录、公开站点、自检清单。局限我也
  清楚：一是行为结论来自读码不是全部经过运行验证，二是动手实验部分我
  目前只跑通了无 GPU 的假引擎链路 🔬，三是还没有向上游提交过 PR。我的
  下一步计划是把书里的对照实验真跑出数字，并向仓库贡献一个小修复，把
  "读懂"升级成"做过"。

- 追问 S1：AI 参与的部分怎么界定？如果面试官问"这是你自己写的吗"？
  **口播**：我会如实回答：这是我主导的研究项目，AI 是我的工具，承担了
  源码定位、结构化初稿和站点搭建，我负责确定问题清单、验证关键论断、
  做技术判断和最终定稿——比如打分公式那章，是我把选择器源码逐段读完
  对着公式核对了每一项和默认值之后才认可的。我认为评价研究项目的标准
  是结论是否正确、方法是否可复核，而不是每个字怎么 produced 的；这本
  书每个论断都能落到具体源码位置，任何人可以验证，这正是我设计它的目
  的。同时我也用行动补 AI 无法替代的部分：动手实验和真实贡献，这是
  我列了明确计划的。

- 追问 S2：这个项目和你应聘的岗位有什么关系？
  **口播**：岗位要求的推理服务优化能力，这个项目给了我三个直接可迁移
  的资产。第一是机制层的完整地图：路由、调度、缓存分层、分离部署这些
  子系统怎么协作、故障面在哪，我在真实生产框架里验证过这套理解。第二
  是方法论：锚定版本的源码考据、代价建模的估算习惯、降档排障的剥离思
  路，这些直接适用于任何推理系统的性能工作。第三是我了解这套代码的
  贡献路径和测试体系，能快速从一个读代码的人变成提 PR 的人。欠缺的是
  生产运维的一手数字，这也是我接下来要补的实验计划。

## 4. 源码证据索引

| 主题 | 关键路径与内部符号 | 对应正文位置 |
|------|--------------------|--------------|
| 打分公式 | `lib/kv-router/src/scheduling/selector/default.rs`：`DefaultWorkerSelector`、`worker_logit()`、`pick_default_worker()`、`softmax_sample_index()`；`scheduling/config.rs:748`：`KvRouterConfig`（默认值在 946 行 `Default` impl） | 面经 Q8、Q9；手册 ch12 |
| 选择调用链 | `lib/llm/src/kv_router/routing_host/kv_selection.rs`：`select_worker_outcome()`、`WorkerSelection`；`lib/llm/src/kv_router.rs:346`：`FindBestMatchOutcome`、`:532`：`KvRouter` | 面经 Q8；手册 ch12 §12.2 |
| 队列策略 | `lib/kv-router/src/scheduling/config.rs:454`：`RouterQueuePolicy`（fcfs/wspt，`DYN_ROUTER_QUEUE_POLICY`）；`scheduling/queue.rs`：`SchedulerQueueActor` | 面经 Q10；手册 ch13 §13.1 |
| 事件管线 | `lib/llm/src/kv_router/publisher/mod.rs:195`：`KvEventPublisher`、`DEFAULT_MAX_BATCH_BLOCKS=128`、`MAX_BATCHING_TIMEOUT_MS=15_000`；`publisher/{batching,dedup,worker_metrics}.rs`（`PUBLISH_DEBOUNCE=1ms`、`ActiveLoad`） | 面经 Q5；手册 ch10 |
| 线格式 | `lib/kv-router/src/zmq_wire/types.rs:88`：`RawKvEvent`（`lora_name`、`cache_salt`、`block_mm_infos`、`is_eagle`）；`lib/kv-router/src/protocols.rs:1229`：`KvCacheEventData` | 面经 Q5、Q7；手册 ch10 §10.2 |
| 索引结构 | `lib/kv-router/src/indexer/README.md`（FlashIndexer 设计文档：四标识符/双结构/选型）；`indexer/radix_tree.rs`：`RadixTree`、`apply_event()`、`dump_tree_as_events()`；`indexer/kv_indexer.rs:297`：`KvIndexer` | 面经 Q6、Q7；手册 ch11 |
| 游标状态机 | `lib/kv-router/src/recovery/cursor.rs`：`CursorState`、`CursorObservation`（Initial/Contiguous/Gap/Stale） | 面经 Q17；手册 ch24 |
| 迁移排除 | `lib/llm/src/protocols/common/preprocessor.rs:153`：`excluded_worker_ids()`；`kv_selection.rs:209` 消费点 | 面经 Q17；手册 ch24 §24.3 |
| LoRA | `lib/bindings/python/rust/llm/lora.rs`：`LoRADownloader`；`lib/llm/src/lora/{controller,predictor,load_estimator,filter,state_tracker}.rs`；引擎侧 `_gen_lora_extra_hash_keys`（vLLM fork） | 面经 Q7 追问；手册 ch23 |
| 共享缓存 | `lib/llm/src/kv_router/shared_cache.rs`（HiCache/SGLang+Mooncake 客户端；`SGLANG_HICACHE_MOONCAKE_RUNTIME_KEY`） | 面经 Q16 追问；手册 ch25 |
| 分离部署 | `lib/kv-router/src/worker_type.rs`：`WorkerType`；`lib/llm/src/kv_router/prefill_router/mod.rs:219`：`PrefillRouter`；`lib/kv-router/src/conditional_disagg.rs`；`examples/backends/vllm/deploy/disagg*.yaml` | 面经 Q13；手册 ch14 |
| 传输 | `lib/kvbm-physical/src/transfer/executor/{nixl,cuda,memcpy}.rs`；`components/src/dynamo/vllm/handlers.py`：`_DeferredAbort` | 面经 Q14、Q15；手册 ch15 |
| 驱逐 | `lib/kvbm-logical/src/pools/inactive/backends/{fifo,lru_backend,multi_lru_backend,lineage/eviction}.rs`、`tinylfu.rs` | 面经 Q11；手册 ch13 |
| 多模态 | `lib/llm/src/preprocessor/media/README.md`（MediaFetcher/MediaDecoder）；`protocols.rs`：`BlockMmObjectInfo`、`RequestExtraInfo::to_block_level` | 面经 Q13 追问；手册 ch22 |
| 部署 | `deploy/operator/internal/controller/dynamographdeployment_controller.go`、`dgd_component_program.go`、`failover_cascade_controller.go` | 面经 Q17、Q18；手册 ch20 |
| 测试 | `tests/serve/common.py:338`：`run_serve_deployment`；`tests/fault_tolerance/`（etcd_ha/migration/canary） | 面经 Q17、Q19；手册 ch26 |
