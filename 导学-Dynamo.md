# 导学-Dynamo（面试导向版）

> 本导学是《Dynamo 学习手册》（本仓库，26 章）的**面试备战投影**：不复述全书，
> 只回答"面大模型推理/推理基础设施岗，按什么顺序学、学到什么深度、去哪验证"。
> 技术事实全部来自手册正文与 `ai-dynamo/dynamo@61e9184` 源码核对。
> 配套题库见《面经-Dynamo.md》。

## 1. 前置知识（面试高频标注）

| 知识点 | 为何需要 | 在本项目中的位置 | 高频度 |
|--------|----------|------------------|--------|
| prefill/decode 两阶段与资源特征 | 一切推理系统设计的出发点 | `01-overview/ch01-llm-inference-primer.md` | ★★★★★ |
| KV cache 容量公式（2×层×KV头×维×长×精度） | 迁移估算、显存规划的口算基础 | ch01 §1.2、ch15 §15.3 | ★★★★★ |
| PagedAttention / 前缀缓存 | 理解"前缀可复用 → 路由感知 KV"的因果 | ch01、ch11 | ★★★★★ |
| 分布式基础（注册发现、pub/sub、RDMA 概念） | 看懂三条通信通路与 KV 传输 | ch06–ch07、ch15 | ★★★★ |
| K8s 基础（CRD/Operator/污点容忍） | 看懂 DGD 渲染与路由 taints | ch20 | ★★★ |
| 调度论基础（FCFS/最短处理时间） | 队列策略取舍 | ch13 | ★★★ |

## 2. 重点亮点与学习顺序（先看这个）

| 亮点标题 | 为什么重要（面试视角） | 通用技术关键词 | 先看哪些文件 | 建议顺序 |
|----------|------------------------|----------------|--------------|----------|
| 缓存感知路由的完整决策公式 | 最容易被深挖的独有素材：多数人只会讲概念，你能讲公式与默认值 | 代价模型、分层计价、softmax 采样、热点衰减 | 手册 ch12 + `../lib/kv-router/src/scheduling/selector/default.rs` | 第 1 |
| 前缀索引双数据结构 | radix tree 是高频题，跳跃优化/选型决策树是加分项 | 压缩前缀树、并发结构、O(D/J) 优化 | 手册 ch11 + `../lib/kv-router/src/indexer/README.md` | 第 2 |
| PD 分离与 KV 迁移成本建模 | 推理岗必考，字节数估算 + 带宽判断是硬通货 | 分离式服务、RDMA、TTFT 分解 | 手册 ch14–ch15 + `../examples/backends/vllm/deploy/disagg.yaml` | 第 3 |
| 尽力而为事件流的可靠性设计 | 体现分布式功力：可检测的最终一致 | 单调序号、游标状态机、幂等丢弃 | 手册 ch10、ch24 + `../lib/kv-router/src/recovery/cursor.rs` | 第 4 |
| 多层 KV 管理与驱逐 | OS 换页类比好讲，准入/驱逐分离是亮点 | 分层缓存、TinyLFU、谱系驱逐 | 手册 ch13、ch16 | 第 5 |
| 版本考据方法 | 展示资料鉴别能力，防自己被旧文档误导 | API 迁移识别、源头验证 | 手册 ch02 §2.4 | 穿插 |

## 3. 必备知识点（checklist）

- [ ] 默写 KV 字节公式并解释每个因子
- [ ] 默写路由打分公式（缓存抵扣三层计价 + 衰减 + decode 特例 + taints 乘子）
- [ ] 说清 7 种路由模式与降档排障路径
- [ ] 四种块标识符的语义；三重盐各自防什么
- [ ] PD 三阶段时序；条件分离的判定思路
- [ ] 游标四态（Initial/Contiguous/Gap/Stale）的处置
- [ ] FCFS vs WSPT 的目标函数差异
- [ ] 价值阶梯 1.0 / 0.75 / 0.5 / 0.25 的来源
- [ ] DGD → Pod 渲染管线；K8s 与 etcd 两层"生死"的分工
- [ ] `run_serve_deployment` 四步流水（体现你能贡献代码）

## 4. 推荐阅读（结合仓库）

手册位于本仓库；Dynamo 源码在上级 `../`（即 `D:\git-package\dynamo`）。

| 主题 | 通用技术点 | 建议阅读位置 | 预计时间 | 读完能回答什么 |
|------|-----------|--------------|----------|----------------|
| 架构定位 | 编排层 vs 引擎 | `01-overview/ch02-what-is-dynamo.md` | 25 min | 为什么有 Dynamo、边界在哪 |
| 请求主线 | 请求八阶段 | `02-core/ch09-frontend-entry.md` | 50 min | 一个请求怎么走完 |
| 路由公式 | 代价模型 | `03-kv-routing/ch12-routing-decision.md` + `../lib/kv-router/src/scheduling/selector/default.rs` | 70 min | 打分每一项的含义与默认值 |
| 索引 | 前缀树/跳跃优化 | `03-kv-routing/ch11-prefix-index.md` + `../lib/kv-router/src/indexer/README.md` | 60 min | 两种结构怎么选 |
| 分离与传输 | 成本建模 | `04-disagg/ch14-pd-disagg.md`、`ch15-nixl-transfer.md` | 90 min | 何时分离划算、要搬多少字节 |
| 容错 | 最终一致检测 | `07-advanced/ch24-fault-tolerance.md` + `../lib/kv-router/src/recovery/cursor.rs` | 45 min | 事件丢了怎么办 |
| 测试 | 回归契约 | `07-advanced/ch26-testing-ci.md` + `../tests/serve/common.py` | 35 min | 怎么给路由改动写验证 |

## 5. 自学提醒

若某文件或原理看不懂，请继续追问 AI；本技能负责给学习路径与题目，不提供逐行讲解。

另外两条针对本项目的提醒：

1. **口播之前先过手**：`01-overview/ch05-getting-started.md` 的 mocker 链路
   不需要 GPU，跑通一次，面试里所有"我验证过"的话才有底气。
2. **要主动讲的 3-5 个点先复核源码**：打分公式、游标四态、价值阶梯——
   对着 `61e9184` 再读一遍，面试时带一句"我锚定在这个 commit"。

## 6. 项目技术定位

AI（推理基础设施）方向 + 交叉（分布式系统）。依据：核心素材是 LLM 推理
框架的分布式架构（路由、传输、缓存、编排），落点是 GPU 集群服务化。

## 7. 核心原理解析（问题 → 机制 → 落点）

1. **请求不知道发给谁** → 名字服务 + lease 注册 + watcher 路由表（etcd，
   无独立心跳，过期即摘除） → `02-core/ch08-discovery.md`。
2. **前缀重复计算浪费** → 块哈希 + 前缀索引 + 代价打分（命中抵扣负载） →
   `03-kv-routing/ch11、ch12`。
3. **事件流可靠性** → 单调事件号 + 游标状态机 + 快照重放兜底 →
   `07-advanced/ch24-fault-tolerance.md`。
4. **两阶段负载互相干扰** → prefill/decode 分池 + KV 跨节点搬运 +
   条件分离兜底 → `04-disagg/ch14、ch15`。
5. **显存装不下 KV** → 分层存储 + 分层计价路由 + 准入/驱逐分离 →
   `03-kv-routing/ch13`、`04-disagg/ch16`。

## 8. 关键设计决策（备选 / 取舍 / 风险 / 验证）

| 决策 | 备选 | 取舍 | 风险 | 验证 |
|------|------|------|------|------|
| 索引允许"错" | 强一致索引 | 换取事件面批处理/去重/压缩空间 | 发错 worker 重算（次优非错误） | 命中率指标 + 未命中回退日志 |
| T=0 蓄水池破平 | 固定 tie-break | 防同分偏向单一 worker | 可复现性略降（有 seed 测试） | `selector/default.rs` 单测 |
| 分离式部署 | 全聚合 | 独立扩缩容、消除互相干扰 | 短请求/差网络反而亏 | 条件分离旁路 + 迁移字节数估算 |
| 路由侧算 prefill 负载 | worker 上报 | 路由知道接受时刻，无滞后 | 路由与 worker 记账可能漂移 | 序列账本对账（`free_if_worker`） |

## 9. 量化与验证（含待测，建议）

- 命中率 → TTFT 收益：用 mocker + 前缀重复 trace，对比 kv 与 round-robin
  两档（**待测**，跑 ch19 的对照实验即可获得第一手数字）。
- 迁移带宽敏感性：按 ch15 公式代入目标网络规格，口算 TTFT 增量（可测：
  分离部署前后压测）。
- 事件面开销：固定负载扫 `DYN_ROUTER_EVENT_THREADS` 与批量参数（**待测**）。

---

下一步：拿《面经-Dynamo.md》逐题过口播；答不顺的题回到本表第 4 节的
对应章节重读。
